 #include <stdio.h>
 #define maxitem 5;
 #define maxweight 20;

int main() {
int names [5];
int values [5];
int weights[5];
    for (int i = 0; i < 5; i++)
    {
        printf("name of the item" ,);
        scanf("%s", &names[i]);
        printf("value of the item" ,);
        scanf("%d", &values[i]);
        printf("weight of the item" ,);
        scanf("%d", &weights[i]);
    }
    
}